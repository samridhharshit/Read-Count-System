{
  "name": "pratilipi",
  "version": "1.0.0",
  "description": "Read Count System",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "start": "node server.js",
    "client": "cd client && npn start",
    "dev": "concurrently -n 'server,client' -c 'red/green' \"node server.js\" \"cd client && npm start\""
  },
  "keywords": [
    "pratilipi",
    "Count",
    "System"
  ],
  "author": "Samridh Harshit",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "concurrently": "^5.3.0",
    "express": "^4.17.1",
    "mongodb": "^3.6.2",
    "mongoose": "^5.10.5",
    "nodemon": "^2.0.4",
    "path": "^0.12.7"
  }
}
